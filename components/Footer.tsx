
const Footer = () => {
    return (
        <footer className="flex justify-center">
            <p className="text-slate-600">
                Gold{` © ${new Date().getFullYear()} `}All rights reserved.
            </p>
        </footer>

    );
}

export default Footer;